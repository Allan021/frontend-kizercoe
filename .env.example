# A qué API le pega el sitio. Se compila dentro del bundle (Astro reemplaza los
# PUBLIC_* al hacer build), así que cambiarla exige volver a compilar el sitio.
#
#   local:      http://localhost:3001
#   producción: https://api.kizercode.com
PUBLIC_API_URL=http://localhost:3001
